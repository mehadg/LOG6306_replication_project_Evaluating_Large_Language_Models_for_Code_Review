feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_equal_to_sum_even(n):
        if n % 2 != 0:
            return False
        for i in range(2, int((n - 1) / 2) + 1):
            if i % 2 == 0 and sum([i, (n - i)]) == n:
                return True
        return False