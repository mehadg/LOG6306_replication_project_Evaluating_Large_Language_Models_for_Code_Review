feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_bored(S):
        sentences = [s for s in S.strip('! .?').split('. ') + S.split('? ') + S.split('! ') if s]
        return sum(1 for sentence in sentences if sentence[0].isupper())