feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def check_dict_case(dictionary):
        if not dictionary:
            return False
        for key in dictionary:
            if not (key.islower() or key.isupper()):
                return False
        return True