feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def tri(n: int) -> List[int]:
        tribonacci = [1, 3, 2]
        for i in range(3, n+1):
            tribonacci.append((tribonacci[-1] + tribonacci[-2] + tribonacci[-3]) if i % 2 != 0 else (1 + i//2))
        return tribonacci