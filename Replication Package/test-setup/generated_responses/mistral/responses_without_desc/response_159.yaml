feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import Iterable

    def find_max(words: Iterable[str]):
        max_word = ""
        max_unique = 0
        for word in words:
            unique = len(set(word))
            if unique > max_unique:
                max_word = word
                max_unique = unique
            elif unique == max_unique and word < max_word:
                max_word = word
        return max_word