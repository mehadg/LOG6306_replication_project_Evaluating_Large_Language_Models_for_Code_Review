feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_sorted(lst):
        if len(lst) <= 1 or all(a <= b for a, b in zip(lst, lst[1:])):
            return True
        else:
            return False