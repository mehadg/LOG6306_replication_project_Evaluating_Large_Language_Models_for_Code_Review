feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def hex_key(num):
        primes = '2357BD'
        count = 0
        for digit in num:
            if digit in set(primes):
                count += 1
        return count