feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def closest_integer(value):
        if value[0] == '-':
            return int(value[1:])
        try:
            return int(value)
        except ValueError:
            # Handle cases where the input cannot be cast to an integer
            return -1