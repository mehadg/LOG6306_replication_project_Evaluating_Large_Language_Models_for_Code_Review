feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def fruit_distribution(s, n):
        s = s.lower()
        num_apples, num_oranges = map(int, s.split(" apples ") + s.split(" oranges")[1].split(" and "))
        return n - num_apples - num_oranges