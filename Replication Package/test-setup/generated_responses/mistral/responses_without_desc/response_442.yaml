feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def odd_count(lst):
        output = []
        for i, s in enumerate(lst):
            odd_num = sum(1 for c in s if c.isdigit() and int(c) % 2 == 1)
            output.append("the number of odd elements {}n the string {} of the list[{}]".format(odd_num, ''.join(s), i+1))
        return output