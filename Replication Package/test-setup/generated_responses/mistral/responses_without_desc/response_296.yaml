feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def digits(n):
        product = 1
        for digit in str(abs(n)):
            if int(digit) % 2 == 1 or int(digit) != '0':
                product *= int(digit)
        return product