feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def rounded_avg(n: float, m: float) -> str:
        if n > m:
            return "-1"
        avg = round((n + m) / 2)
        return format(avg, 'b')