feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def file_name_check(file_name):
        valid_extensions = [".txt", ".exe", ".dll"]
        if re.search(r"[a-zA-Z]", file_name) and (file_name.endswith(tuple(valid_extensions)) or file_name == file_name.upper()):
            return 'Yes'
        else:
            return 'No'