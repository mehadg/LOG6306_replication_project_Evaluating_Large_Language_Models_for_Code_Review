feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def next_smallest(lst: List[int]):
        if not lst or len(lst) < 2:
            return None
        sorted_set = sorted(set(lst))
        return sorted_set[1]