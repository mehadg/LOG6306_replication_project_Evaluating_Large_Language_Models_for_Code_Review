feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_palindrome(text: str) -> bool:
        if len(text) <= 1:
            return True
        elif text[0] != text[-1]:
            return False
        else:
            return is_palindrome(text[1:-1])