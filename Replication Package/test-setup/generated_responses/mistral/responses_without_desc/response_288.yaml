feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def get_odd_collatz(n):
        res = []
        while n != 1:
            if n % 2 != 0:
                res.append(n)
            n = (n // 2) if n % 2 == 0 else (3*n + 1)
        res.append(1)
        res.sort()
        return res