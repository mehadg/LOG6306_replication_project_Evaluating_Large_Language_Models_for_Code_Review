feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def vowels_count(s):
        count = sum(1 for c in s if c.lower() in ['a', 'e', 'i', 'o', 'u']) + (1 if s and s[-1].lower() == 'y' else 0)
        return count