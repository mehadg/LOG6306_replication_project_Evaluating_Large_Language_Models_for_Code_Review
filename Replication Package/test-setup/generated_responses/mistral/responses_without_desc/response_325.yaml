feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def do_algebra(operator: List[str], operand: List[int]):
        result = operand[0]
        for i in range(len(operator)):
            op = operator[i]
            opd = operand[i+1]
            if op == '+':
                result += opd
            elif op == '-':
                result -= opd
            elif op == '*':
                result *= opd
            elif op == '//':
                result //= opd
            elif op == '**':
                result **= opd
        return result