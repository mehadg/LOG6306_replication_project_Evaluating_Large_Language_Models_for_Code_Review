feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def encrypt(s):
        def rotate(c):
            return (chr((ord(c) - 97 + 4) % 26 + 97) if c.isalpha() else c)
        return ''.join(rotate(c) for c in s)