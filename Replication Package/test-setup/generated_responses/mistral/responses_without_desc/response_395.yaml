feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def digitSum(s):
        sum = 0
        for c in s:
            if c.isdigit():
                sum += int(c)
            elif c.isupper():
                sum += ord(c)
        return sum