feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    import math

    def max_fill(grid: List[List[int]], capacity: int) -> int:
        rows = len(grid)
        cols = len(grid[0])
        count = 0
        for i in range(rows):
            for j in range(cols):
                if grid[i][j] == 1:
                    count += math.ceil(1 / capacity)
        return count