feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def count_nums(arr: List[int]):
        count = 0
        for num in arr:
            if sum(abs(num) for digit in str(num)) > 0:
                count += 1
        return count