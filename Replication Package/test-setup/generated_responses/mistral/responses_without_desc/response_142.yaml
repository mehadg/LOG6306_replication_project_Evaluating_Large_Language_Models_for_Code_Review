feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def file_name_check(file_name):
        pattern = r"[a-zA-Z]+\d{0,3}\.txt|exe|dll$"
        if re.search(r"[a-zA-Z]", file_name) and re.search(pattern, file_name):
            return 'Yes'
        else:
            return 'No'