feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def prod_signs(arr):
        if not arr: return None
        prod = 1 if all(i >= 0 for i in arr) else -1
        return prod * sum(arr)