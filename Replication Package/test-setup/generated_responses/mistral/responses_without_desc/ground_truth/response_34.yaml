feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def sort_third(l: List[int]):
        l = sorted(l[::3]) + sorted(l[1::3]) + sorted(l[2::3])
        return l