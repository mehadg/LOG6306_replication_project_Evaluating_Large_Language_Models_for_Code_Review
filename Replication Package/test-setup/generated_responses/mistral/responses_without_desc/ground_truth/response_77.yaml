feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_simple_power(x, n):
        if x == 1 or n == 1:
            return x == 1
        power = 1
        while power < x:
            power *= n
        return power == x