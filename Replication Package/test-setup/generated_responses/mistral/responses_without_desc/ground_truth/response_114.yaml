feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def odd_count(lst):
        res = []
        for arr in lst:
            n = sum(i%2==1 for i in arr)
            res.append(f"the number of odd elements is {n} in the array {arr}")
        return res