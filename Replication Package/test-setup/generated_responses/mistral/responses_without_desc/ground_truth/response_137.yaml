feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    def largest_smallest_integers(lst):
        smallest = min(lst, key=lambda x: float('inf') if x >= 0 else x)
        largest = max(lst, key=lambda x: -float('inf') if x <= 0 else x)
        return (smallest, largest)