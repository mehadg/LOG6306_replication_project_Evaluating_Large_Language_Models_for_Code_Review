feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def common(l1: list, l2: list):
        ret = set()
        for e1 in l1:
            if e1 in l2:
                ret.add(e1)
        return sorted(list(ret))