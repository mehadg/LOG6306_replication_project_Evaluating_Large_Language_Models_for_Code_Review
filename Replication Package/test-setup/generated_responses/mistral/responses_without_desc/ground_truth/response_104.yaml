feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def rounded_avg(n, m):
        if m < n:
            return -1
        total = sum(range(n, m+1))
        binary_result = bin(round(total / (m - n + 1)))
        return binary_result