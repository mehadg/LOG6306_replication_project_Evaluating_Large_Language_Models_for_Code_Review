feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def valid_date(date):
        try:
            date = date.strip()
            month, day, year = date.split('-')
            month, day, year = int(month), int(day), int(year)
            if (month < 1 or month > 12) or \
               (month in [4,6,9,11] and day < 1 or day > 31) or \
               (month in [1,3,5,7,8,10,12] and day < 1 or day > 31 and day > 28) or \
               (month == 2 and (day < 1 or day > 29)):
                    return False
            return True
        except ValueError:
            return False