feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def pairs_sum_to_zero(lst):
        from itertools import combinations
        return any([sum(pair) == 0 for pair in combinations(lst, 2)] if lst else False