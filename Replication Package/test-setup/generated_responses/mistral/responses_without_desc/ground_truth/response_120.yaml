feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def match_parens(lst):
        def check(s):
            val = 0
            for char in s:
                if char == '(':
                    val += 1
                elif char == ')':
                    val -= 1
                if val < 0:
                    return False
            return val == 0

        S1 = lst[0] + lst[1]
        S2 = lst[1] + lst[0]
        result = 'Yes' if check(S1) or check(S2) else 'No'
        return result