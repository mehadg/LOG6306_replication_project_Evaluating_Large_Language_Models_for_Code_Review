feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def get_row(lst: List[List], x: int):
        coords = [(i, j) for i in range(len(lst)) for j in range(len(lst[i])) if lst[i][j] == x]
        return sorted(coords, key=lambda x: (x[0], -x[1]))