feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def string_xor(a: str, b: str) -> str:
        def xor(i, j):
            return ('0' if i == j else '1')

        return ''.join(xor(x, y) for x, y in zip(a, b))