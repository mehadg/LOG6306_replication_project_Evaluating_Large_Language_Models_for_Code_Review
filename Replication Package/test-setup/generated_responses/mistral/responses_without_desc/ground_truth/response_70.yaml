feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def search(lst):
        from typing import List
        max_num = max(lst)
        frq = [0] * (max_num + 1)
        for i in lst:
            frq[i] += 1

        ans = -1
        for i in range(1, len(frq)):
            if frq[i] >= i:
                ans = i

        return ans