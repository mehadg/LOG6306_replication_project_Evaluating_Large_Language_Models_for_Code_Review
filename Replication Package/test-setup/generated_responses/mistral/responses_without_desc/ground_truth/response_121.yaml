feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def maximum(arr, k):
        if k == 0:
            return []
        arr.sort(reverse=True)
        ans = arr[:k]
        return ans