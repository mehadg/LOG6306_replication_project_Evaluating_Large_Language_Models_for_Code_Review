feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_prime(n):
        import math

        def _is_prime(p):
            if p < 2:
                return False
            for k in range(2, int(math.sqrt(p)) + 1):
                if p % k == 0:
                    return False
            return True

        sequence = [0, 1]
        while n > len(sequence):
            sequence.append(sum(sequence[-2:]))
            if _is_prime(sequence[-1]):
                n -= 1
        return sequence[-1]