feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def compare_one(a, b):
        a = float(a.replace(",", ".")) if a.isalnum() else a
        b = float(b.replace(",", ".")) if b.isalnum() else b

        if isinstance(a, (int, float)) and isinstance(b, (int, float)):
            if a == b:
                return None
            elif a > b:
                return a
            else:
                return b
        else:
            return a if a == b else max(a, b)