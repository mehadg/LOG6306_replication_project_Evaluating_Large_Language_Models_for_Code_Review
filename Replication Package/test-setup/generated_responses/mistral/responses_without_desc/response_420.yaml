feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_bored(S):
        sentences = [sentence for sentence in (S.split('. '), S.split('? '), S.split('! ')) if sentence]
        return sum(1 for sentence in sentences if sentence[0].strip().lower() == 'i')