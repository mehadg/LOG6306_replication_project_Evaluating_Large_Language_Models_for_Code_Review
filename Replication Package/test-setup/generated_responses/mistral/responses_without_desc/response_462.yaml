feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List, TypeVar
    T = TypeVar('T')

    def sum_squares(lst: List[T]) -> int:
        return sum([math.ceil(x) ** 2 for x in lst])