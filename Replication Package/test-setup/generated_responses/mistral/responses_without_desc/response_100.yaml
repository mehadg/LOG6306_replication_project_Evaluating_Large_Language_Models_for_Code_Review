feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def closest_integer(value):
        value = str(value)
        if value[0] == '-':
            return int(value[1:])
        elif '.' in value:
            return int(str(round(float(value))) )
        else:
            return int(value)