feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def solution(lst: List[int]):
        return sum(num for num in lst if num % 2 != 0 and len(lst) % 2 == 1 or num % 2 != 0 and num < len(lst))