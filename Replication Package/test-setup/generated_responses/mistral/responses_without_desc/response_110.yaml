feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def move_one_ball(arr):
        if not arr:
            return True
        for i in range(1, len(arr)):
            if sorted(arr[i:]) + arr[:i] == arr:
                return True
        return False