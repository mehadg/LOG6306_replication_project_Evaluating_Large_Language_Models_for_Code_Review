feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def choose_num(x, y):
        if x > y:
            x, y = y, x
        max_even = -1
        for i in range(min(x, y), max(x, y)+1):
            if i % 2 == 0 and i > max_even:
                max_even = i
        return max_even