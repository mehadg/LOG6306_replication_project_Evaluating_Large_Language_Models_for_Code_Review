feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def fibfib(n: int):
        if n < 0:
            return "Error: Input should be a non-negative integer"
        elif n == 0 or n == 1:
            return n
        else:
            a, b = 0, 1
            for i in range(2, n+1):
                a, b = b, a + b
            return b