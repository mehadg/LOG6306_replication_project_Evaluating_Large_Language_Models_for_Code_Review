feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def maximum(arr, k):
        if len(arr) <= k:
            return arr
        else:
            arr.sort(reverse=True)
            return arr[:k]