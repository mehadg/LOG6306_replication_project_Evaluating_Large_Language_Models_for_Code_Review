feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def simplify(x, n):
        from fractions import Fraction
        x = Fraction(x)
        n = Fraction(n)
        result = x * n
        if result.denominator == 1:
            return True
        else:
            return False