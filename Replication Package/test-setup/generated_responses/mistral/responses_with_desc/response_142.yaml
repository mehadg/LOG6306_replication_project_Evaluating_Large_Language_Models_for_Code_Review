feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def file_name_check(file_name):
        if len(re.findall(r'\d', file_name)) <= 3 and re.search(r'^[a-zA-Z]+\.', file_name) and re.search(r'(txt|exe|dll)$', file_name[file_name.index('.'):]):
            return 'Yes'
        else:
            return 'No'