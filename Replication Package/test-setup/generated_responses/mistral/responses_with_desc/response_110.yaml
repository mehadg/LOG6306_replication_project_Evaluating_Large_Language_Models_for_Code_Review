feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def move_one_ball(arr):
        if len(arr) == 0:
            return True

        arr = list(sorted(arr))
        for i in range(len(arr)):
            if arr[:i] + arr[i:] == arr:
                return True
        return False