feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_palindrome(text: str):
        if len(text) <= 1:
            return True
        if text[0] != text[-1]:
            return False
        return is_palindrome(text[1:-1])