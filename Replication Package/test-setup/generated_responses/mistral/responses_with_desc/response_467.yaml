feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_equal_to_sum_even(n):
        for i in range(2, n):
            if n - i - (n-i)%2 - ((n-i)//2)%2 - ((n-i)//4)%2 == 0:
                return True
        return False