feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def below_zero(operations: List[int]):
        balance = 0

        for op in operations:
            balance += op
            if balance < 0:
                return True

        return False