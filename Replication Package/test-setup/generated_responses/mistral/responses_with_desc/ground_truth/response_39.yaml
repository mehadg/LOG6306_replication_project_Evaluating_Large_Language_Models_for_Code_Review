feedback:
  classified_type: |
    Incorrect
code:
  complete_code: |
    def encode_cyclic(s: str) -> str:
        result = ""
        for i in range(len(s) // 3):
            group = s[i*3 : (i+1)*3]
            result += group[-1] + group[0] + group[1]
        if len(s) % 3 != 0:
            result += s[-1]
        return result