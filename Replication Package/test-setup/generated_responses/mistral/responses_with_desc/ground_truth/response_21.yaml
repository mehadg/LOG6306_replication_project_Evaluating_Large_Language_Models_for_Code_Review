feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def find_closest_elements(numbers: List[float]):
        closest_pair = None
        distance = None

        for idx, elem in enumerate(sorted(numbers)):
            for elem2 in sorted(numbers)[idx:]:
                if distance is None:
                    distance = abs(elem - elem2)
                    closest_pair = (elem, elem2)
                else:
                    new_distance = abs(elem - elem2)
                    if new_distance < distance:
                        distance = new_distance
                        closest_pair = (elem, elem2)

        return closest_pair