feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def exchange(lst1, lst2):
        odd_lst1 = sum(1 for i in lst1 if i % 2 != 0)
        odd_lst2 = sum(1 for i in lst2 if i % 2 == 0)
        return "YES" if odd_lst2 >= odd_lst1 else "NO"