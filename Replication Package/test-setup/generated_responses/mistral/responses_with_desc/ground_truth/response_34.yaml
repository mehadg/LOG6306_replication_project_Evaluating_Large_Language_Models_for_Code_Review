feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def sort_third(l: List[int]) -> List[int]:
        l[:] = [i if i not in range(0, len(l), 3) else sorted(l)[i] for i in range(len(l))]
        return l