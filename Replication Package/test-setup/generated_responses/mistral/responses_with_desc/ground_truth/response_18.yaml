feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def parse_music(music_string: str) -> List[int]:
        note_map = {'o': 4, 'o|': 2, '.|': 1}
        music_list = [note_map.get(x, None) for x in music_string.split(' ') if x]
        return [beat for beat in music_list if beat is not None]