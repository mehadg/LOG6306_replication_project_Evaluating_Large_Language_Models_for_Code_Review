feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def sort_array(array):
        if len(array) == 0:
            return []
        sorted_array = sorted(array, reverse=(array[0] + array[-1]) % 2 != 0)
        return sorted_array