feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def change_base(x: int, base: int):
        if base < 2 or base > 10:
            raise ValueError("Invalid base value")
        ret = ""
        while x > 0:
            ret = str(x % base) + ret
            x //= base
        return ret