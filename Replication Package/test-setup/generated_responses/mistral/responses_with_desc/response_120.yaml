feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def match_parens(lst):
        open_count = 0
        close_count = 0

        for string in lst:
            for c in string:
                if c == '(':
                    open_count += 1
                elif c == ')':
                    if open_count > 0:
                        open_count -= 1
                    else:
                        close_count += 1

        return 'Yes' if open_count == 0 and close_count == 0 else 'No'