feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def prime_fib(n: int):
        a, b = 0, 1
        while True:
            a, b = b, a + b
            if is_prime(b) and n > 1:
                n -= 1
                if n == 1:
                    return b