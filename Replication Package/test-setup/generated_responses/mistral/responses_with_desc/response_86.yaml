feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def add(lst: List[int]):
        return sum(x for i, x in enumerate(lst) if i % 2 == 1 and x % 2 == 0)