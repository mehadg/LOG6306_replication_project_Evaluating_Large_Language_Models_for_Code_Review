feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def modp(n, p):
        result = 1
        base = 2
        while n > 0:
            if n % 2 == 1:
                result = (result * base) % p
            n = n // 2
            base = (base * base) % p
        return result