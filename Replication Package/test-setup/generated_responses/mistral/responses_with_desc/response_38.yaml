feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def sort_even(l: List[int]) -> List[int]:
        return [num if i % 2 else sorted(l[i - 1:i + 1])[0] for i, num in enumerate(l)]