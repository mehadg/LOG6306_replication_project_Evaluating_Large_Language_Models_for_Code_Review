feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def specialFilter(nums: List[int]):
        return sum(1 for num in nums if num > 10 and str(num)[0].isodd() and str(num)[-1].isodd())