feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def file_name_check(file_name):
        if re.search(r"^[a-zA-Z][a-zA-Z\d]{0,2}\.[txt|exe|dll]$", file_name) and len(re.findall(r"\.", file_name)) == 1:
            return 'Yes'
        else:
            return 'No'