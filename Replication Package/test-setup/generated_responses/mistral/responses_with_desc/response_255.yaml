feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def next_smallest(lst):
        sorted_list = sorted(set(lst))
        if len(sorted_list) < 2:
            return None
        else:
            return sorted_list[1]