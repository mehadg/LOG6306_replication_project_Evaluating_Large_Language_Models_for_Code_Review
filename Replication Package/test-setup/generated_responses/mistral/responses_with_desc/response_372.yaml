feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def pairs_sum_to_zero(numbers: List[int]):
        return any(num1 + num2 == 0 for num1, num2 in zip(numbers, numbers[1:])) or any(num == 0 for num in numbers if num != 0)