feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def monotonic(lst: List[int]):
        increasing = True
        for i in range(1, len(lst)):
            if lst[i] < lst[i-1]:
                increasing = False
                break
        decreasing = not increasing
        return increasing or decreasing