feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def search(lst):
        counts = {}
        for x in lst:
            if x in counts:
                counts[x] += 1
            else:
                counts[x] = 1
        max_val = -1
        for x in sorted(counts, key=counts.get, reverse=True):
            if counts[x] > x:
                max_val = x
        return max_val