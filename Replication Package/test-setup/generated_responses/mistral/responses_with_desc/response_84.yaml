feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def count_numbers(n):
        start_one = [str(1) + x for x in range(1, int('1' * n))]
        end_one = [x[::-1].replace('0', '', 1) for x in list(range(10 ** (n - 1), 10 ** n)) if x[0] == '1']
        return len(start_one + end_one)