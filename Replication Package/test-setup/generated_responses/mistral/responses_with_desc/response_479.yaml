feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def x_or_y(n, x, y):
        if n < 2 or n > 1 and all(n % i != 0 for i in range(2, int(n**(1/2))+1)):
            return x
        else:
            return y