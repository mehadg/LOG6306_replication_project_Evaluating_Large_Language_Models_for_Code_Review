feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def can_arrange(arr):
        for i in range(1, len(arr)):
            if arr[i] > arr[i-1]:
                continue
            else:
                return i
        return -1