feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def vowels_count(s):
        count = 0
        for c in s:
            if c.lower() in ['a', 'e', 'i', 'o', 'u'] or (c.lower() == 'y' and len(s) == 1):
                count += 1
        return count