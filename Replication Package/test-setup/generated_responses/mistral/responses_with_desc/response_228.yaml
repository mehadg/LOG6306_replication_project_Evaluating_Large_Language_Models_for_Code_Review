feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def fibfib(n):
        if n < 0:
            raise ValueError("n must be non-negative")

        if n <= 2:
            return 1 if n == 2 else 0

        a, b = 0, 1
        for _ in range(3, n+1):
            a, b = b, a + b
        return b