feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_bored(S):
        sentences = [s for s in S.split('. ') + S.split('? ') + S.split('! ')]
        return sum(1 for sentence in sentences if sentence.strip().startswith('I'))