feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def search(lst: List[int]):
        counts = {}
        for x in lst:
            if x in counts:
                counts[x] += 1
            else:
                counts[x] = 1
        result = -1
        for x in sorted(counts, key=counts.get, reverse=True):
            if counts[x] >= x:
                result = x
        return result