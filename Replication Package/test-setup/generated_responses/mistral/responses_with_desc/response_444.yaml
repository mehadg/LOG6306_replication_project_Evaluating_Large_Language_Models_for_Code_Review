feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    import math

    def max_fill(grid, capacity):
        rows = len(grid)
        cols = len(grid[0])
        count = 0
        for i in range(rows):
            for j in range(cols):
                if grid[i][j] == 1:
                    water_to_remove = min(capacity, grid[i][j])
                    count += math.ceil(water_to_remove)
        return count