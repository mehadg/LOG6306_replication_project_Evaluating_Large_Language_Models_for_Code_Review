feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    def order_by_points(nums: List[int]):
        nums = sorted(nums, key=lambda x: (sum(abs(int(digit)) for digit in str(x)), nums.index(x)))
        return nums