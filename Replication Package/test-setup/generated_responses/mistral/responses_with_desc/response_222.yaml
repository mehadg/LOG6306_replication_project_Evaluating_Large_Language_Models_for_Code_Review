feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def monotonic(l: List[int]) -> bool:
        return all(x <= y for x, y in zip(l, l[1:])) or all(x >= y for x, y in zip(l, l[1:]))