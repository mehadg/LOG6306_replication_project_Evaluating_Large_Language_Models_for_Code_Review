feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def sort_array(array):
        if not array:
            return []
        sum_of_ends = array[0] + array[-1]
        sorted_array = sorted(array)
        if sum_of_ends % 2 == 0:
            return sorted_array[::-1]
        else:
            return sorted_array