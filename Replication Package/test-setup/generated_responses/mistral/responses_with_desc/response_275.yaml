feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def exchange(lst1, lst2):
        even_lst = [num for num in lst2 if num % 2 == 0]
        odd_lst = [num for num in lst1 if num % 2 != 0]
        return "YES" if len(even_lst) >= len(odd_lst) else "NO"