feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def sort_array(array):
      if not array:
        return []
      return sorted(array, reverse=(array[0] + array[-1]) % 2 == 0)