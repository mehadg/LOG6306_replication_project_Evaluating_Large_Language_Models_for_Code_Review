feedback:
  classified_type: Correct
code:
  complete_code: |
    from typing import List
    
    def triples_sum_to_zero(l: List) -> bool:
        return any(sum(x, y, z) == 0 for i in range(len(l)) for j in range(i + 1, len(l)) for k in range(j + 1, len(l)) if x := l[i], y := l[j], z := l[k])