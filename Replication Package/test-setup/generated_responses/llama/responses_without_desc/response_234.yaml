feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def search(lst: List[int]) -> int:
        counts = {}
        for x in lst:
            if x in counts:
                counts[x] += 1
            else:
                counts[x] = 1
        result = -1
        for x in sorted(counts, reverse=True):
            if x <= list(counts.values())[0]:
                result = x
        return result