feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def encode_shift(s: str):
        return "".join([chr((ord(ch) + 5 - ord("a")) % 26) + chr(97) if "a" <= ch <= "z" else ch for ch in s])
    
    def decode_shift(s: str):
        return "".join([chr((ord(ch) - 5 - ord("a")) % 26) + chr(97) if "a" <= ch <= "z" else ch for ch in s])