feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def do_algebra(operator: List[str], operand: List[float]) -> float:
        result = operand[0]
        for op, opd in zip(operator, operand[1:]):
            if op == '+':
                result += opd
            elif op == '-':
                result -= opd
            elif op == '*':
                result *= opd
            elif op == '//':
                result //= opd
            elif op == '**':
                result **= opd
        return result