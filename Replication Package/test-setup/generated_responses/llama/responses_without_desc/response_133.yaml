feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def is_nested(string):
        stack = []
        for bracket in string:
            if bracket == "[":
                stack.append(bracket)
            elif bracket == "]":
                if len(stack) == 0:
                    return False
                elif stack[-1] == "[":
                    stack.pop()
                else:
                    return True
        return False