feedback:
  classified_type: |
    Incorrect
code:
  complete_code: |
    def is_equal_to_sum_even(n):
      for i in range(2, n//2 + 1):
        if i % 2 == 0 and sum([j for j in range(i, n//2 + 1) if j % 2 == 0]) == n:
          return True
      return False