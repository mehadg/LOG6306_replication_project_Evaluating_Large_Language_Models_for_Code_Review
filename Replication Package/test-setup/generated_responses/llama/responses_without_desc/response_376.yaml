Here is the feedback:

feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def median(l: List) -> float or int:
        l.sort()  
        n = len(l)  
        if n % 2 == 1:  
            return l[n // 2]  
        else:  
            return (l[n // 2 - 1] + l[n // 2]) / 2