feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def min_sub_array_sum(nums):
      min_sum = float("inf")
      current_sum = 0
      for num in nums:
          current_sum += num
          if current_sum < min_sum:
              min_sum = current_sum
      return min_sum