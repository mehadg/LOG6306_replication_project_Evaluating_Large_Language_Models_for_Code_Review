feedback:
  classified_type: |
    Incorrect
code:
  complete_code: |
    from typing import List
    
    def sorted_list_sum(lst: List) -> int:
        return sum(int(word) for word in (word for word in lst if len(str(word)) % 2 == 0))