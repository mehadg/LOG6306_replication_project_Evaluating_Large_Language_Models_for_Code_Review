feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def prod_signs(arr):
      if not arr: return None
      sign = -1 if any(x < 0 for x in arr) else 1
      return sign * sum(abs(i) for i in arr)