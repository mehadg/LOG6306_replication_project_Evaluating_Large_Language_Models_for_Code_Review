feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def hex_key(num):
      primes = ['2', '3', '5', '7', 'B', 'D']
      total = 0
      for i in range(len(num)):
        if num[i] in primes:
          total += 1
      return total