feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def prime_length(s):
      if len(s) < 2:
        return False
      for i in range(2, int(len(str(len(s))) ** 0.5 + 1)):
        if len(s) % i == 0:
          return False
      return True