feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def median(l: List) -> float:
        l = sorted(l)
        if len(l) % 2 == 1:
            return l[len(l) // 2]
        else:
            return (l[len(l) // 2 - 1] + l[len(l) // 2]) / 2.0