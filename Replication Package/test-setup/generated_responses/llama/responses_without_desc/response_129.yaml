feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def prod_signs(arr):
      if not arr:
        return None
      product = 1
      sum_of_magnitudes = 0
      for i in arr:
        product *= i
        sum_of_magnitudes += abs(i)
      return sum_of_magnitudes * product