feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def sum_squares(lst):
        return sum(num ** ((i+1) % 3 + (i+1) % 4) for i, num in enumerate(lst))