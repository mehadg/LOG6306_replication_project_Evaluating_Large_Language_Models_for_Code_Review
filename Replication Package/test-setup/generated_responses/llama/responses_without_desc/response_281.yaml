Here is the feedback:

feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def sort_array(arr):
        return sorted(arr, key=lambda x: (bin(x).count("1"), x))