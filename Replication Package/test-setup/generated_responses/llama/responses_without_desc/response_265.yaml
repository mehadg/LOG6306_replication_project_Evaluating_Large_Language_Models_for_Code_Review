feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def make_a_pile(n: int) -> List[int]:
        pile = []
        for i in range(1, n+1):
            if i % 2 == 0:
                pile.append(n + (i-1))
            else:
                pile.append(n + i)
        return pile