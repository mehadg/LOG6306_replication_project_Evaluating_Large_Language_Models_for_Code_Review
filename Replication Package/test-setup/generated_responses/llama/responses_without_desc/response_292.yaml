feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def intersection(interval1, interval2):
      start = max(interval1[0], interval2[0])
      end = min(interval1[1], interval2[1])
      if start <= end:
        return f"YES ({start} to {end})"
      else:
        return "NO"