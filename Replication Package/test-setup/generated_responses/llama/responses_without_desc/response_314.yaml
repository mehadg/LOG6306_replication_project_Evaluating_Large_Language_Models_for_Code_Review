feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def sorted_list_sum(lst: List[str]) -> int:
        return sum(int(word) for word in sorted([word for word in lst if len(word) % 2 == 0]))