feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def solve(N):
      binary = bin(N)[2:] 
      binary_sum = sum(int(digit) for digit in binary)
      return bin(binary_sum)[2:]