feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def order_by_points(nums):
        return sorted(nums, key=lambda x: (abs(x), str(abs(x)).index(str(x))))