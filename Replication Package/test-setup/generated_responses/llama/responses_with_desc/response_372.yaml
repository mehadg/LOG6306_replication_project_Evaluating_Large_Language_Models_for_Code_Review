feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def pairs_sum_to_zero(l: List[int]) -> bool:
        return any(abs(num) in l for num in set(l)) and len(set(l)) > 1