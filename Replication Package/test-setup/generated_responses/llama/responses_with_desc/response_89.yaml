feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def sort_array(array):
      if not array:
        return sorted(array)
      elif (array[0] + array[-1]) % 2 == 0:
        return sorted(array, reverse=True)
      else:
        return sorted(array)