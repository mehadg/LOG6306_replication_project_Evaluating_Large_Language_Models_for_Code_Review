feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def check_dict_case(dict):
      if len(dict) == 0:
        return False
      else:
        for key in dict:
          if not isinstance(key, str):
            return False
          elif not (key.islower() or key.isupper()):
            return False
        return True