feedback:
  classified_type: |
    Incorrect
code:
  complete_code: |
    def encode_cyclic(s: str):
      if len(s) % 3 == 0:
        result = ""
        for i in range(0, len(s), 3):
          group = s[i:i+3]
          new_group = group[1] + group[2] + group[0]
          result += new_group
        return result
      else:
        raise ValueError("Input string is not a multiple of three characters")