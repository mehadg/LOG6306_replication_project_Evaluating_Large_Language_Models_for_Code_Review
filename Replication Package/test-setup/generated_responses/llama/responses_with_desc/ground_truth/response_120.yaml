feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def match_parens(lst):
      s1 = lst[0] + lst[1]
      s2 = lst[1] + lst[0]
      return 'Yes' if (s1.count('(') == s1.count(')') and s2.count('(') == s2.count(')')) else 'No'