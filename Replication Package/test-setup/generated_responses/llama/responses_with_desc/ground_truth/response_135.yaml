feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def check_if_last_char_is_a_letter(txt):
        if txt:
            last_char = txt.split()[-1]
            return len(last_char) == 1 and 'a' <= last_char.lower() <= 'z'
        else:
            return False