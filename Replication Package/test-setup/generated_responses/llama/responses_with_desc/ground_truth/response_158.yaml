Here is my feedback:

feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def right_angle_triangle(a, b, c):
      return (a*a + b*b == c*c) or (a*a + c*c == b*b) or (b*b + c*c == a*a)