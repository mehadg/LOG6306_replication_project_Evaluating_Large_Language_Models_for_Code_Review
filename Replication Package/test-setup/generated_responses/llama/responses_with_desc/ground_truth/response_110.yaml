feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def move_one_ball(arr):
      if len(arr) == 0:
        return True
      sorted_array = sorted(arr)
      for _ in range(len(arr)):
        arr = [arr[-1]] + arr[:-1]
        if arr == sorted_array:
          return True
      return False