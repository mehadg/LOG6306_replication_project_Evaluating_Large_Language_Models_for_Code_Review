feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def any_int(x, y, z):
      if isinstance(x, int) and isinstance(y, int) and isinstance(z, int):
        return (x+y==z) or (x+z==y) or (y+z==x)