feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def strange_sort_list(lst):
      res, switch = [], True
      while lst:
        if switch:
          res.append(min(lst))
          lst.remove(min(lst))
        else:
          res.append(max(lst))
          lst.remove(max(lst))
        switch = not switch
      return res