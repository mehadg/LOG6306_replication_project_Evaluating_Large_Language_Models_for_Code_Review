feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def vowels_count(s):
      vowels = "aeiouAEIOU"
      n_vowels = sum(1 for c in s if c in vowels)
      if len(s) > 0 and s[-1].lower() == 'y':
        n_vowels += 1
      return n_vowels