feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List

    def factorize(n: int) -> List[int]:
        factors = []
        i = 2
        while i <= int(pow(n, 0.5)) + 1:
            if n % i == 0:
                factors.append(i)
                n //= i
            else:
                i += 1

        if n > 1:
            factors.append(n)
        return factors