feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def encrypt(s):
      d = 'abcdefghijklmnopqrstuvwxyz'
      out = ''
      for c in s:
        if c.lower() in d:
          idx = (d.index(c.lower()) + 8) % 26
          out += d[idx]
        else:
          out += c
      return out