feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List, Tuple
    
    def find_closest_elements(numbers: List[float]) -> Tuple[float, float]:
        closest_pair = None
        distance = None
        
        for elem in numbers:
            for other_elem in numbers:
                if elem != other_elem:
                    if distance is None:
                        distance = abs(elem - other_elem)
                        closest_pair = tuple(sorted([elem, other_elem]))
                    else:
                        new_distance = abs(elem - other_elem)
                        if new_distance < distance:
                            distance = new_distance
                            closest_pair = tuple(sorted([elem, other_elem]))
        
        return closest_pair