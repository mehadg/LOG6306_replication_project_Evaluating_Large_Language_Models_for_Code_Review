feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def circular_shift(x, shift):
      s = str(x)
      if shift >= len(s):
        return s[::-1]
      else:
        return s[shift:] + s[:shift]