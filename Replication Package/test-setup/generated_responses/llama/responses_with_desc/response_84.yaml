feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def starts_one_ends(n):
      if n == 1:
        return 1
      else:
        return (n == 1 or str(10**(n-1)).endswith('1')) + starts_one Ends(n - 1)