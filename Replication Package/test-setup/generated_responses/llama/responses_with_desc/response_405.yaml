feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_simple_power(x, n):
        if n == 0 and x != 1:
            return False
        root = int(round(x ** (1. / float(n))))
        return root ** n == x