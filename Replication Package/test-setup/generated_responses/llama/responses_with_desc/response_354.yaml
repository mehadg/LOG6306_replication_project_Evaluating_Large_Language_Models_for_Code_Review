feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    from typing import List
    
    def factorize(n: int) -> List[int]:
        factors = []
        current_number = n
        divisor = 2
        while current_number > 1:
            if current_number % divisor == 0:
                factors.extend([divisor] * (current_number // divisor ** (current_number // divisor).bit_length()))
                current_number = 1
            else:
                current_number -= 1
                divisor += 1
        return factors