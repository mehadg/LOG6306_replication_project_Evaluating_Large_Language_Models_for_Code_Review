feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def move_one_ball(arr):
      if len(arr) == 0:
        return True
      for i in range(len(arr)):
        arr = arr[-1:] + arr[:-1]
        if arr == sorted(arr):
          return True
      return False