Here is my feedback:

feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def compare_one(a, b):
      try:
        a = float(str(a).replace(",", "."))
      except ValueError:
        pass
      try:
        b = float(str(b).replace(",", "."))
      except ValueError:
        pass

      if isinstance(a, (int, float)) and isinstance(b, (int, float)):
          if a == b:
              return None
          elif a > b:
              return a
          else:
              return b
      else:
          return a if a == b else max(a, key=str)