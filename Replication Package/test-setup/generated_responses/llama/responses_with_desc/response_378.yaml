Here is the feedback:

feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def modp(n: int, p: int):
      if p == 1:
        return 0
      result = 1
      base = 2 % p
      while n > 0:
          if n % 2 == 1:
              result = (result * base) % p
          n = n // 2
          base = (base * base) % p
      return result