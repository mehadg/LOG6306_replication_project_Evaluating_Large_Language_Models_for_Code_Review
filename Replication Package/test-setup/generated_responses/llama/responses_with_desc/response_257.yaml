feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def any_int(x, y, z):
        return isinstance(int(x), int) and isinstance(int(y), int) and isinstance(int(z), int) and (x == y + z or y == x + z or z == x + y)