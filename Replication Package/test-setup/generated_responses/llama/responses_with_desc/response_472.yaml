feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def words_in_sentence(sentence):
      sentence = sentence.split()
      result = [word for word in sentence if len(word) > 1 and is_prime(len(word))]
      return ' '.join(result)