Here is the feedback:

feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def greatest_common_divisor(a: int, b: int) -> int:
        if a == 0 or b == 0:
            return abs(a)
        elif a % b == 0:
            return b
        else:
            return greatest_common_divivor(b, a % b)