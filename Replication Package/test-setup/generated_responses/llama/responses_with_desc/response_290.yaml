feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    import re
    
    def split_words(txt):
        if ' ' in txt:
            return txt.split()
        elif ',' in txt:
            return txt.replace(',', ' ').split()
        else:
            return sum(1 for x in txt if ord(x.lower()) % 2 != 0)