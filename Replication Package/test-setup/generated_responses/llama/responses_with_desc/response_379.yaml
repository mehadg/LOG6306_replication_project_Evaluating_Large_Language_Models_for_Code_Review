feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def function_name(parameters):
      if parameters == "encode":
        return "".join([chr(((ord(ch) + 5 - ord("a")) % 26) + ord("a")) for ch in s])
      elif parameters == "decode":
        return "".join([chr(((ord(ch) - 5 - ord("a")) % 26) + ord("a")) for ch in s])