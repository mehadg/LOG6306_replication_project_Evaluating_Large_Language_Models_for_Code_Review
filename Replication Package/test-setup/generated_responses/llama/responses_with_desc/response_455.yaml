Here is my response:

feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def is_sorted(lst):
      for i in range(len(lst) - 1):
        if lst[i] > lst[i + 1]:
          return False
      return len(set(lst)) == len(lst)