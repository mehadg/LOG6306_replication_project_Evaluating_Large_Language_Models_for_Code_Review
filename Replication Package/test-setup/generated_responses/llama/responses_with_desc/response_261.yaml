Here is my response:

feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def count_up_to(n: int) -> list:
        if n < 2:
            return []
        primes = [2]
        for i in range(3, n):
            is_prime = True
            for j in range(2, int(i ** 0.5) + 1):
                if i % j == 0:
                    is_prime = False
                    break
            if is_prime:
                primes.append(i)
        return primes