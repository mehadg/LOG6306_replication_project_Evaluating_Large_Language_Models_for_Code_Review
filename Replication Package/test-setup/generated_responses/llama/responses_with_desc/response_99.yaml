feedback:
  classified_type: |
    Correct
code:
  complete_code: |
    def count_upper(s):
      vowels = 'AEIOU'
      count = 0
      for i, char in enumerate(s):
        if i % 2 == 0 and char.upper() in vowels:
          count += 1
      return count